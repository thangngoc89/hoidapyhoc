<div class="jumbotron">
    <div class="container">
        <h2 class="jumbotron__heading">{{ $name }}</h2>
        <h4 class="jumbotron__sub-heading">{{ $heading }}</h4>

        <a href="{{ $link }}" class="btn btn-primary">
            <i class="{{ $class }}"></i> {{ $button }}
        </a>
    </div>
</div>